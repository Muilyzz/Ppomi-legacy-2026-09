{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Main window may run one brain/path/body spine command and proxy one Gateway Responses turn.",
  "windows": ["main"],
  "permissions": ["core:default", "allow-run-path", "allow-ai-gateway"]
}
