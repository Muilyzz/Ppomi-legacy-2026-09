# UI 트리 — 요구 조건 장부와 삭제 기록

머스크의 5단계를 작업대 트리에 돌린 기록이다. 순서는 (1) 요구 조건을 의심한다: 항목마다 요구한 사람의 이름이 붙어야 한다.
(2) 지운다: 지운 것의 10%를 되살리지 않았다면 덜 지운 것이다. (3) 그다음에야 단순화한다. (4) 주기를 줄인다. (5) 마지막에 자동화한다.
구조의 원본은 루트 `storybook/`(`cd storybook && npm run storybook`)의 **작업대**·**대화 셸** 스토리이고, 뼈대는 `agent/src/ui/`다.
Mac·Android는 이 구조를 따라 맞춘다(스토리북 먼저, 그다음 동일 적용).

## 1. 요구 조건 장부 (누가 · 무엇을)

| 요구 | 주인 | 판정 |
|---|---|---|
| 제어 대상 창이 앉는 빈 자리를 UI 안에 둔다 | 사용자 ("미러링 앱 공간을 비우는 UI") | 유지 |
| 큰 데이터 뷰가 필요하면 대상 창을 숨기고 기록을 넓게 본다 | 사용자 ("작업 타겟 윈도우를 숨기면 안되나") | 유지 = 기록 페이지 |
| 말은 최소, 색은 둘, 글꼴 하나, 다크, 큰 글자 | 사용자 | 유지 |
| Mac·Android 구조가 같다 | 사용자 | 유지 |
| 채팅과 음성 둘 다 | 사용자 | 유지 = 방식 탭 |
| 사람 차례(승인·질문)는 눈에 띄게, 코드 경계 안에서 | 사용자(결제 승인 규칙) | 유지 = 차례 띠 |
| 기록 요약 띠가 항상 위에 보인다 | 없음 (내가 "신뢰"를 이유로 넣음) | **삭제** |
| 기록 띠의 펼침(36%) | 없음 | **삭제** |
| 기록 띠의 양보(헤더 한 줄) | 없음 (내가 만든 충돌의 땜질) | **삭제** |
| 두 열 머리띠는 대칭 | 없음 (내 취향) | **삭제** (대화 열 머리띠) |
| 대화 열 아래 상태 푸터 | 없음 | **삭제** (셸 상태 줄이 이미 있음) |
| 제어 자리 "대기" 테두리 | 없음 | **삭제** (아무 일도 없을 때 표시할 것 없음) |
| 두 열 50/50 | 없음 | **삭제** → 제어 열 폭 = 대상 창 폭 |
| 셸 헤더(뽀미 + ⚙) | 없음 | **삭제** (제목 중복, 설정은 네이티브 설정으로) |
| 셸 안 서버 주소 폼 | 없음 | **삭제** → Mac 설정 창 · Android 설정 탭 |
| Android 기기 상태 aside + 허용 앱 목록 | 없음 | **삭제** → 미연결은 오류 띠 한 줄, 허용 앱은 대상 선택기 |
| 세션 안내문 | 없음 | **삭제** |
| 대화 열의 "저장한 내용"(기억) | 없음 | **삭제** |
| 기록 페이지의 기억 탭 | 없음 (row 위의 이동은 내 결정, 네이티브 기억 뷰 없음) | **삭제** |
| 제어 머리띠 새로고침 | 없음 (네이티브 미러링 앱이 스스로 재연결) | **삭제** |
| 제어 자리 "진행" accent 테두리 | 없음 (구멍에 폰 화면이 보이므로 진행은 이미 보임) | **삭제** → 상태 셋 |
| onSaved 콜백 · records.ts Memory 타입 | 없음 (기억 UI 삭제 뒤 남은 죽은 코드) | **삭제** |
| 셸 grid-area · --wait 토큰 · 셸 스토리의 workbench 스킨 | 없음 (지운 것의 잔해) | **삭제** |
| 음성은 대화 안의 통화(전화처럼 걸고·받고·끊기) | 사용자 ("페이스타임 하듯이 … 보이스톡 앱같은걸") | 유지 = 📞 버튼 · 통화 바 · 통화 카드 |
| 통화 중 주고받은 말이 로그에 말풍선으로 남음 | 사용자 (같은 요구) | 유지 = 통화 세션 전사(`callMessagesFromHistory`, 글 대화 투영은 그대로 음성 제외) |
| 사람 차례는 비서처럼: 먼저 톡, 답 없으면 재촉, 그래도 없고 급하면 전화 | 사용자 ("비서처럼, 먼저 톡에 용건을 넣은다음에 전화를 걸게 하자. … 답장이 없는데 급하게 결정해야 하는 경우에만 전화") | 유지 = 단계 셋. 0분 톡(`ppomiNotice` 말풍선 + 조용한 알림 "뽀미 · 용건") → 1분 재촉(알림 "아직 기다려요") → 3분 전화(`ppomiIncomingCall` + Mac 벨 / Android OS 통화). 벨은 45초면 부재중으로 끊고 톡은 남는다. 차례가 풀리면 모두 취소. Mac `Kiosk.askSub`, Android `VoiceSessionHost.turn`. 급한 정도(마감·되돌림 불가)로 단계를 당기는 건 요구 없음. 일일 결산 보고는 톡만, 전화 없음(정례 결산은 별도 성질) |
| 정례 결산(일일 보고) | 사용자 ("일일 결산 보고같은건 또 다른 성질") | 유지 = Mac 매일 21:00 `today_spending` 본문을 톡으로 한 장(`DailyReport`, `Kiosk.reportTimer`). 사람 차례가 아니므로 재촉·전화 없음. Android는 장부가 없어 해당 없음. 시각 설정은 요구 없음 |
| 걸려온 통화 받기 | 사용자 ("걸려온걸 받는 기능") | 유지 = 수신 띠. 트리거는 위 단계의 3분째(디버그는 즉시)가 `ppomiIncomingCall(용건)` 호출, 해결되면 `""`로 내림. 나중에 = `declineCall`(Android OS 벨도 끊김). 받기 = 통화가 열리고 뽀미가 용건을 한 문장으로 말한다. 승인·선택 자체는 네이티브 버튼(코드 경계)이고 통화는 설명만 한다. 앱이 꺼져 있을 때 걸려오기는 서버 측 에이전트가 생길 때(LiveKit/SIP 판단 보류) |
| 벨소리 + 시스템 알림 | 사용자 ("사운드가 안뜨니 오는지 모르겠네. 노티도 안뜨고") | 유지 = 페이지가 Web Audio로 울림(440·480Hz, 30초), Mac UNUserNotification. Android는 OS 전화(Telecom self-managed, `PpomiTelecom`)라 벨·진동·잠금 화면은 OS 몫 — 전체 화면 알림 권한(USE_FULL_SCREEN_INTENT)이 있어야 잠금 화면 위에 뜬다 |
| Android 전화 모드(카카오톡 보이스톡처럼) | 사용자 ("받았더니 잠금 그대로고 … 뺨 터치 보호 … 전화모드는?") | 유지 = `CallActivity` 하나: 잠금 화면 위에서 울림(거절·받기) → 받으면 잠금을 풀지 않고 그대로 통화 화면(연결 중 → 통화 중 m:ss · 스피커 · 대화 보기 · 끊기). 걸 때도 같은 화면. 귀에 대면 근접 센서로 화면 끔(수화기일 때만, 스피커·다른 화면에선 안 끔). 진행 중 알림을 누르면 이 화면으로 돌아온다 |
| 통화 중 승인은 말로도(구두 결재) | 사용자 ("승인도 그냥 전화 대답으로 하게 하던지 … 교본엔 어때") + 교본(구두 결재 = 복창 + 기록, 돈은 사후 서면) | 유지 = 뽀미가 금액·대상·수단을 복창하고 "승인이라고 말씀하시면 진행합니다"라고 청한다. 사람의 전사(`heard`)에 "승인"/"취소" 하나만 있으면 기기가 차례의 그 선택지를 누른다(`VoiceApproval`; 모델은 아무것도 승인하지 못한다). "네"만으로는 안 됨. 기록 = 전사 말풍선 + "말로 승인 · 용건" 말풍선. 잠긴 Android에선 무시(잠금 화면 결제 승인 금지) → "대화 보기"로 잠금 해제 |
| 통화 화면에 차례 띠 없음 | 규칙 (잠금 화면에서 결제 승인 금지) | 유지 = 잠긴 채로는 말로도 버튼으로도 안 된다. "대화 보기"가 잠금 해제를 요구한 뒤 대화(네이티브 버튼)를 연다 |
| 뽀미가 건 전화는 받자마자 뽀미가 먼저 말함 | 사용자 ("먼저 전화를 했으니 여보세요 하면 대답이 바로 있어야지") | 유지 = 받기 → 세션이 붙자마자 `response.create`(용건 한 문장, 용건 없으면 인사). 붙는 데 몇 초 걸리는 동안은 "연결 중" — 벨 울리는 동안 미리 붙이기는 요구 생기면 |
| 예약·판단으로 걸기(앱이 꺼져 있어도) | 사용자 (2026-09-10 논의) | 보류·설계만: 판단은 서버 규칙(납부일 전날·약 시간·승인 방치), LLM은 `schedule_call`로 예약만. 타이머 = Supabase pg_cron → Vercel 엔드포인트 → Android FCM; Mac은 상시 앱이라 표를 폴링해 스스로 울림(APNs는 서명 상태로 불가). iPhone 해당 없음 |
| 로그 = 현재 세션 | 규칙 (종료 시 대화 삭제) | 통화 카드·전사는 다음 글 대화가 시작될 때 함께 사라진다. 한 로그에 여러 토막을 남기려면 고정 사본이 필요 — 요구 없음 |
| 끝 카드의 길이·요약 | 없음 (요약 생산자 없음, 1분 미만은 거짓) | **삭제** |
| 카드·수신 띠의 장식 📞 | 없음 (입력창 📞만 주인 있음) | **삭제** |
| 음소거 코드 · 수신 출력 잔재 · 음성 입력 워크릿 · 애니메이션 | 없음 (지운 UI의 잔해) | **삭제** |
| 상태 띠의 × (미설정·미연결) | 없음 (초점 복귀 때 이미 재확인) | **삭제** |
| 방식 탭(채팅|음성) | 없음 (통화 모델이 대체) | **삭제** |
| orb · 음성 무대 · 음성 바 · "음성 시작" | 없음 (통화 바 한 줄 + 끊기가 대체) | **삭제** |
| 첫 화면 힌트 + 제안 하나 | 사용자(어르신) | 유지 |
| 닫힌 폴드·작은 Mac 창에서는 제어 열이 레일 한 줄로 접히고, 펼친 폴드·큰 창에서는 왼쪽이 닫힌 화면 그대로 + 오른쪽 제어 열 | 사용자 ("통일성", 2026-09-10) | 유지 = 크기 클래스 둘(840px 컨테이너 쿼리), 차례 띠는 접히지 않음 |
| 아이패드는 뷰어: 탭 한 줄(타임라인·증빙·분개) + 페이지 하나가 화면 전부. 대화 열 없음(키보드 없는 기기 — 타자 대신 뒤에 통화), 설정은 톱니(구글 로그인) 하나 | 사용자 ("타자 입력보다는 뷰어와 대화 위주", "대부분의 공간을 정보 표현에", 2026-09-10) | 유지 = `iPad/Web/pad.html` + `pad.css`. 로그인 = 구글(Supabase Auth PKCE, 콜백 `ppomi://auth` 공통). 기기 등록 = 구글 계정 = 구성원(`ppomi_members`, `ppomi_register_device`). 장부 = Supabase 암호화 기록(ledger·evidence·accounting)을 기기가 직접 읽어 Mac 과 같은 페이지로(`PadVault`: `LedgerPage`, evidence html, `journal.html`+`journal.js`); 기록 키는 서버에 평문으로 없다: 기기마다 X25519 공개키를 등록하고(`ppomi_register_device p_public_key`), 키를 가진 Mac 이 기다리는 기기의 공개키로 감싼 사본만 올린다(`ppomi_devices_waiting`·`ppomi_key_wrap_put`, 1분마다·활성화 때, `GoogleAccount.exchangeKeys`); 받는 기기는 비밀키로 푼다(`KeyWrap`, ppomi-wrap-v1). 사용자 결정 2026-09-11: "키를 Vault 에 안 넣고 더 암호화된 방식" — 서버·소유자·백업 모두 못 풀고, 새 기기는 Mac 이 켜져 있을 때 받는다. 작업대 스토리(대화+장부 열)는 통화가 붙을 때의 골격. 조종·제어 자리 없음, 3D 탭 안 가져감 |
| Mac 뽀미는 보통 앱: Dock 아이콘 + 표준 메뉴만. 커스텀 "뽀미" 메뉴 없음 — 명령은 대화(도구: 수집은 `collect_now`)와 창 안(기록·초록 확대·프로필 아이콘; 제어 화면 선택기는 없음 — 어느 창을 데려올지는 도구 호출이 정한다), 설정값은 설정 창(음성: 뽀미야 듣기·연결 시 먼저 말하기) | 사용자 ("메뉴바 엑스트라는 없어도 되는 식으로 심플하게", "뽀미 속에 또 뽀미 메뉴가 있는게 이상해", "지금 수집도 에이전트스럽지 않다", 2026-09-10) | 유지 = `PpomiApp.swift` Settings 씬만(Help 그룹 비움), CFBundleName 뽀미. 설정 진입 = 제어 머리띠 끝의 톱니(아이패드와 같은 자리, `ControlTargetToolbar`); 메뉴에는 아무것도 더하지 않는다(사용자: "아이패드랑 통일성"). 삭제한 항목: 대화(=Dock 클릭)·대화창 ⌥Space(=📞)·제어 대상(=선택기)·기록 탭들(=기록)·키오스크 ⌃⌘F(=초록 확대)·장부 새로 읽기(=LedgerMonitor)·지금 수집(=대화 "수집해"). 자동 수집(도착·정례)은 요구 생기면 |
| 설정 › 계정 = 구글 로그인 하나(아이패드와 같은 UX). Mac 도 구글 세션 + X-Ppomi-Device 로 서버에 붙고, 옛 기기 계정(이메일·비밀번호 파일)은 첫 로그인 때 기기·작업 공간을 넘기는 데 한 번 쓰고 끝 | 사용자 ("로그인이 또 핵심이니, 아이패드랑 같은 UX/UI로", 2026-09-11) | 유지 = `Shared/SupabaseAuth.swift`(PKCE·토큰·키체인, 두 타깃 공용) + `GoogleAccount`(Mac) / `PadAuth`(iPad). 삭제: 구글 계정 연결 버튼, 공유 서버 섹션(호스트·기기 ID·연결 확인), 에이전트 서버 주소 필드(상수 `PpomiServer.agentEndpoint`, CLI 로만 덮어씀) |
| 설정에 남은 것: 시작하기(손·눈·마이크(통화)·미러링 권한 — 폰 조종에 필수), 계정(구글 로그인), 자동입력 기본정보 | 사용자 | 유지. 삭제(2026-09-11): 글자 크기 → OS 텍스트 크기, 화면 보조 → 서버 결정, 연결(OpenAI 키) → 서버 경유, 음성 → 깨우기 말 기능째 삭제·도착 인사는 뽀미 판단, 장부 → DB 경로는 자동 감지(개발자는 PPOMI_DB), 내 이름은 "나" 프로필의 이름(예금주 캡처·profile_save) → 옛 저장값 → STYLE_ME |
| 프로필 아이콘 = 나(제어 머리띠 끝, 아이패드 오른쪽 위): 로그인 전 실루엣 → 로그인 시트, 뒤엔 구글 사진 → 계정 + 자동입력 프로필. 프로필 섹션은 자물쇠로 잠겨 있고 "지문으로 열기"를 눌러야 보인다. 프로필은 "나" 하나(가족은 각자 로그인) | 사용자 ("자물쇠 버튼과 시트", "유저 프로필 아이콘", "정말로 진실에 가깝게, 지문 있어야만", "각자가 로그인할텐데", 2026-09-11) | 유지 = `Views/MeSheet.swift` + `AvatarView`; 개인정보는 `IdentityVault`(Secure Enclave P256 키, biometryCurrentSet·없으면 userPresence)로 봉인: 쓰기는 공개키(지문 없이), 읽기는 SE 비밀키(지문 뒤에서만, 5분 재사용). 루트·키체인 복사·다른 Mac 으로도 못 읽음. "내 이름"만 지문 없이 필요해 저장 때 따로 적음. 삭제: 프로필 추가·새로고침 버튼, 톱니, 설정 창의 계정·프로필 |
| Mac 작업대 열: 대화는 왼쪽에 폰 폭(412)으로 고정, 제어는 창의 나머지(대상 창 폭과 무관, 대상 창은 그 안 위쪽 가운데에 제 크기). 평상시(뽀미가 폰을 만지지 않을 때)엔 제어 창을 데려오지 않고 기록(상태 뷰)이 제어 열에 뜬다; 폰·Windows 창은 도구가 만질 때(뽀미 진행 중)·사람 차례·큰 화면 사용 때만 데려온다(`KioskController.controlWanted`). 평상시엔 힌트 줄도 없다(기록이 제어 열 전부); 힌트 한 줄은 데려와야 하는데 창이 없을 때만 | 사용자 ("채팅창을 폰 스타일로 왼쪽에 매우 좁게, 나머지 우측 공간을 고정, 제어가 없을 땐 상태 뷰", "무조건 미러링 윈도우를 가져오지 말고 툴콜로 필요해질 때만", 2026-09-11) | 유지 = `WorkbenchLayout.dashboard`(conversationWidth), `WorkbenchContent.statusInSlot`, `RecordsView`의 "← 대화"는 전체 페이지일 때만. 기록 버튼의 전체 페이지 모드는 그대로 |
| 타임라인 계좌 그룹(렌즈): 순자산 머리띠의 칩 "내 것 전부 · 가계부 · 사업… · + 그룹". 고른 그룹의 계좌만 순자산·잔액표·관측 증감에 더한다 — 새 계좌가 관측되기 시작해도 그 계좌가 든 그룹에서만 합계가 뛴다. 계좌 배치 = 페이지 아래 "계좌 배치" 구역의 계좌 칩을 그룹 칩으로 끌어 놓기(계좌는 한 그룹에만, 미분류 가능). Mac만 편집(`lenses.json`, ledger.db 옆), 아이패드는 서버 ledger 기록에 실려 온 그룹을 보기만(배치 구역 숨김) | 사용자 ("그룹 속에 계좌 넣는게 빨리 구현되어야 타임라인에서 자산 점프하는듯하게 보이는 문제가 해결되지", 2026-09-11) | 유지 = `Ledger/LensStore.swift`, `Web/timeline.html` lenses/assign, `SharedLedgerArchive.lenses`, `TimelineView.receive` lens/assign/newLens |
| 타임라인 첫 화면 = "지금": 고른 그룹의 현재 상태. 큰 숫자 하나(순자산 = 본 잔액의 합) → 언제 본 숫자인지("9/2–9/3에 본 잔액 · 9일 전", 가장 오래된 값의 나이) → 자산 / 부채(본 적 없음 — 카드·대출 화면을 읽은 적 없으니 0원이라 쓰지 않음) → **판**(제어 열 전부, 격자 좌표계): 그룹 상자는 격자 위 자유 좌표(끌어 옮김, lenses.json x·y), 계좌 노드는 상자 안(소속) 또는 판 위(미분류, nodes x·y); 노드를 끌어 상자에 넣거나 판 위로 빼기, 노드의 가운데가 놓인 상자가 소속; 상자 머리를 누르면 그 그룹만(순자산·차트가 그 그룹 합) → 못 본 곳 한 줄. 아이패드는 같은 판을 읽기만. 시간에 따른 변화(추이·기간·미관측)는 이 구역에 섞지 않는다 — 다음 단계 | 사용자 ("모든 것은 회계이고 유저는 재무상태표 같은 스냅샷을 보려 하겠지", "둘 중에 먼저 하나만", "그룹핑이 개인 가계부엔 핵심", "쪼매낳게 하지 말고 전체 뷰에 다이어그램적으로", "그룹을 절대 그리드 좌표에 배치하고 계좌 노드를 그리드 속에 넣거나 밖에 빼거나", 2026-09-11) | 유지 = `Web/timeline.html` #now/drawNow/board, `Ledger/LensStore.swift`(lenses+nodes), `WebPageDOMTests.testLensChipsFilterTheSheetAndAssignmentsReachTheApp` |
| **기록 화면 = 재무 상태 트리맵 하나**(탭·차트·하루 패널·판은 일단 숨김/제거): 자산 + 비용 = 부채 + 자본 + 수익. 맨 위는 차변(자산·비용) | 대변(부채·자본·수익) 두 반쪽이 같은 면적, 반쪽 안에서 갈래는 위아래 띠(반쪽 너비 그대로, 높이 = 금액; 얇으면 얇은 선으로 두고 맨 아래 얇은 띠의 글자는 위로 삐져나옴), 갈래 속부터 트리맵. 다섯 갈래는 대표색(자산 파랑 #2f6fed · 비용 주황 #e8792b · 부채 빨강 #d33a3a · 자본 초록 #1f9d6a · 수익 청록 #0e9aa7)을 옅은 음영(26%에서 시작, 자식은 조금씩 더 옅게)으로 깔고, 표에서는 줄 배경 10% 음영으로만 종류를 구분(작은 색 네모 없음). 누르면 그 칸이 맵 전부가 되어(경로 빵조각) 계정과목 → 계좌로 내려간다: 자산 › 예금 › 가계부/사업자 › 계좌, 자산 › 투자 › 가계부 › 증권 계좌(삼성증권 mPOP "총 자산", 카카오페이증권 "계좌 총 자산": 증권 앱은 Rules.investApps, 평가금액 스냅샷); 비용 › 가계부/사업자 › 용도(유지·관계…); 수익 › 가계부/사업자 › 이자수입 등; 자본 › 가계부/사업자. 사업 구분은 계좌에 고정된 사실(기업뱅킹에서 읽은 계좌 = 사업자, Rules.businessApps; 렌즈로 끌어 만드는 실체 kind는 없앰)이고 비용·수익 줄은 반대편 내 계좌의 구분을 따른다. 내 계좌끼리는 이체(비용도 수익도 아님); 가계↔사업 이체는 출자·인출인데 따로 적지 않는다 — 구분마다 자본 = 그 구분의 자산 + 비용 − 수익 − 부채(잔여)로 두면 그 값에 이미 들어 있고 전부에서는 상쇄된다. 부채 = 음수 잔액 계좌(마이너스 통장·대출: 신한 슈퍼SOL 수집 규칙의 debt="대출"이 잔액을 음수로 저장) › 차입 › 가계부/사업자 › 계좌, 양수로 표시; 하나도 없으면 "본 적 없음". 구분의 자본이 음수(부채 > 자산 + 비용 − 수익)면 대변에 그릴 면적이 없으므로 그 몫을 차변 "자본잠식"으로 둔다: 자산 + 비용 + 자본잠식 = 부채 + 자본 + 수익(등식 줄도 그렇게 쓴다). 아래 한 줄이 등식을 숫자로 쓴다. 면적은 스냅샷(지금 잔액·누적 흐름); 시간축(면적 차트)은 다음에 버튼 뒤로. 머리띠의 "트리맵" 토글을 끄면 같은 나무가 차분한 숫자 표로 — 파일 탐색기식 개요: 차변 열 | 대변 열 안에서 계정과목을 누르면 그 자리에서 하위가 들여쓰기로 펼쳐지고(이동 없음, 경로는 늘 "전부") 다시 누르면 접힘(▸/▾); 내역이 없으면 비용·수익은 "내역 없음" — 면적이 없던 것(부채 0원 → 본 적 없음, 본 잔액 없는 계좌)도 여기서는 보여 트리맵의 약점을 보완; 선택은 localStorage. 부동산은 공간 자산에 금액이 생기면 자산 아래 갈래로(취득가 = 등기부등본 거래가액 + 취득세 + 중개수수료; 등기부등본 PDF는 registry_read 도구로 소재지·소유자·거래가액·근저당을 읽고, 인터넷등기소 열람은 Mac에서 TouchEn nxKey 설치·비회원 인증·캡차·700원 결제가 모두 당사자 차례) | 사용자 ("탭도 다 필요없고 정말 트리맵만", "차변 대변으로 면적을 나눠 음영 색", "자산을 누르면 세부 계정과목 → 계좌·부동산 트리로, 그 속에서도 꽉 찬 트리맵", "면적 자체가 스냅샷이니 스냅샷부터", 2026-09-11) | 유지 = `Web/timeline.html` eqTree/drawMap/squarify, `RecordsView`(탭 숨김), `WebPageDOMTests.testEquationMapZoomsFromDebitAndCreditDownToAccounts` |
| 판은 두 층: **사업자** — 사실이지 배치가 아니다: 기업뱅킹 앱(KB스타기업뱅킹, 수집 키 `KBBIZ`, `Rules.businessApps`)에서 사업자로 로그인해 읽은 계좌가 그대로 사업자 상자(읽기 전용, 끌어 넣기·빼기·상자 옮기기 없음, `D.business`). "언제부터"는 신고일·개설일이 들어올 때 / **가계부** — 늘 있는 바닥 하나(사업자 계좌가 아닌 전부 = 개인), 그 안에서만 **렌즈**(용도 그룹: 생활비·저축…, 시간 없음·소급·자유). 사업자여도 신고 안 한 통장은 개인 통장; 그 통장의 사업 경비는 분개 층의 일. **법인은 판에 없다**: 법인은 사람의 장부가 아니라 제 작업공간(Supabase workspace + 구성원 로그인, 소유권 이전 가능 — Vercel·Supabase 팀 방식)이어야 하므로, 만들 때 별도 작업공간으로. 판 왼쪽 = 가계부 바닥(머리를 누르면 가계부만), 그 안에 렌즈 상자와 미분류 노드; 오른쪽 = 사업자 칸(사업자 계좌가 있을 때만, "기업뱅킹에서 읽은 대로"). 칩: 내 것 전부 · 가계부 · 렌즈들 · 사업자 · + 그룹. 순자산 칸: 전부 = 사업자 단위(가계부 하나 + 사업자마다), 가계부를 고르면 렌즈 단위(+ 미분류), 하나를 고르면 그 칸. "가계부" 이름의 사용자 상자는 읽을 때 지운다(바닥이니까). 용도가 시간에 따라 바뀐 계좌: 기본은 소급(이름표만 바뀐 것); 정말 날짜가 중요하면 실체와 같은 "언제부터"를 렌즈 소속에도 선택으로 붙일 수 있게 남겨 둠(아직 안 만듦) | 사용자 ("가계부 그룹을 1개가 항상 존재하게 하고, 렌즈는 그 속에서 다시 그룹핑", "개인사업자·법인은 정해진 것 = 사실 기반의 시간축, 렌즈는 용도", "일단 법인 카테고리는 빼자 — 개인 로그인으로 법인을 보는 건 말이 안 됨", "사업자 칸으로 드래그하는 게 말이 안 되지 — 이미 사업자 계좌가 있는 것", "KB스타기업뱅킹에 내 개인사업자 정보 있어, 필요해", 2026-09-11) | 유지 = `Ledger/LensStore.swift`(home·entity), `Web/timeline.html` drawNow/seriesList/lensInside, `LensStoreTests`, `WebPageDOMTests` |
| 순자산 = 정사각 패킹: 그룹마다 정사각형, 한 변 = √지금 금액(면적 = 금액), 큰 것부터 왼쪽, 나머지는 오른쪽에 위→아래로 쌓아 열을 채우고 전체를 열 너비·최대 높이에 한 번 축척. 가로세로가 늘 같고 칸 안 여백도 칸 크기의 비율(좌우 4%, 위 16%, 아래 12%)이라 그래프는 선이 아니라 바닥 0의 면적 그래프(면적 = 돈 × 시간)라 선 굵기 없이 순수 기하로 줄어든다; 제목(이름·금액)만 고정 크기 오버레이 — 한 줄에 다 들어가면 전부, 아니면 이름만(… 없이, 작은 칸에선 삐져나와도 그대로); 올리면 다 보인다. 판 전체에 테두리 하나 더(붙은 곳과 바깥이 다 두 줄). 마우스: 큰 칸에서만 반응(세로 줄은 모든 차트에, 금액은 각 차트의 값 높이에 판 위 오버레이로(글자의 가운데 선이 그 값의 높이, 배경 없이 바탕색 글자 그림자로만 가독성) — 작은 칸 밖으로 나가도 보임, 날짜는 넓은 칸에서 세로 줄 끝 바로 밑의 축 띠에 하나). 하루 단위 양자화: 면적은 날마다 계단 하나(그날 끝 24:00 값), 마우스도 그날 끝으로 맞춤 — 시각 단위 굴곡은 그리지 않는다; 너무 작은 네모(48px 미만)는 올려도 반응 없음, 누르면 커짐. 작은 그룹은 작은 정사각형 — 올리면(마우스) 또는 머리를 누르면(아이패드) 21rem 정사각형으로 커져 선이 보인다. 본 잔액 없음·음수 그룹은 "면적 밖" 한 줄. 그룹을 고르면 그 칸이 전부. 마우스 올리면 모든 칸에 같은 날짜 줄과 값. 트리맵·원 패킹은 거쳤다가 버림(트리맵: 가로세로가 제각각, 원: 선 영역이 절반) | 사용자 ("트리맵 말고 원이나 정사각 형태… 가로세로가 일정해서 좋을 거", "정사각 패킹으로 하자", 2026-09-11) | 유지 = `Web/timeline.html` packSquares/drawNetWorth/drawTile/zoomTile, `WebPageDOMTests` square·zoom 검사 |
| 도구 카드(로그 안, 시간순) | 사용자(무슨 일이 있었는지) | 유지, 접힘(실패는 펼침) |
| 대화 열의 상태 한 단어(role=status) | 없음 (채팅은 스트리밍 '…'·도구 요약·질문 카드에 이미 보임) | **삭제** → "설정에서 서버 주소"만 오류 띠로. 음성 무대는 한 단어를 둔다(아래 되살린 것) |
| 대화 열 머리 종료 버튼 | 없음 (네이티브가 패널 닫힘 때 ppomiVoiceStop을 부름) | **삭제** (사람이 세울 수단이 필요하면 사용자 주인으로 되살림 — 10% 후보) |
| tokens.css .num 규칙 | 없음 (agent/src에 쓰는 곳 없음, 페이지 스킨 theme.css가 자체 정의) | **삭제** |
| /v1/memories/delete 서버 경로 + Mac·Android 허용 경로 | 없음 (기억 탭 삭제 뒤 클라이언트 없음) | **삭제** — 스트림 밖(agent/server, AgentNative.swift, VoiceBridgePolicy.java), 해당 담당 |
| Shell `voice` prop → `.voice-mode` 클래스 | 없음 (어디에도 선택자 없음, 예전 음성 레이아웃의 잔해) | **삭제** |
| `ConversationTop` 감싸개 + `.conversation-top` | 없음 (종료 버튼 삭제 뒤 자식 하나짜리 flex) | **삭제** → 방식 탭을 `top`에 직접, `.mode-tabs { align-self: flex-start }` |
| 첫 화면 제목 h1 "무엇을 할까요?" | 없음 (35행은 힌트 + 제안 하나만 이름함, 힌트와 목적 중복) | **삭제** |
| 음성 음소거 / 마이크 켜기 버튼 + `muted` 상태 | 없음 (16행은 음성 방식 자체지 음소거가 아님) | **삭제** — `VoiceController.mute()`는 voice.ts(스트림 밖, voice.test.ts가 덮음)에 남아 그 담당의 죽은 코드 |

되살린 것(10% 규칙):
- **정지** — 주인: 사용자(결제 승인 규칙 · 코드 경계, 17행). 종료 버튼 삭제 뒤 폰을 누르는 뽀미를 사람이 세울 길이 없었다(방식 탭은 진행 중 잠김, 보내기는 대기 중 잠김, ppomiVoiceStop은 패널 닫힘뿐). 새 요소 없음: 채팅은 대기 중 보내기 버튼이 정지로 바뀌고, 음성은 진행 중 음성 바에 정지 하나. 둘 다 stopCurrent.
- **음성 무대의 한 단어** — 주인: 사용자(말은 최소·큰 글자·어르신, 14·35행) + 접근성 기본. 연결 중 / 듣는 중 / 말하는 중 / 진행 중. orb의 유일한 신호는 숨쉬기 속도인데 reduced-motion이 그걸 끄고, role=status에 읽을 글자가 없었다. `.voice-stage` 안 `<p>` 하나. 채팅은 그대로 없음.
- 후보: "연결 끊김" 한 줄 — 제어 자리 안에만 둔다.

## 2. 삭제 뒤의 트리

```
작업대                              div.workbench
├─ 작업 공간                         div.workspace   열: 대화 = 남는 폭 · 제어 = 대상 창 폭
│  ├─ 대화     section.conversation  Shell: 수신 띠(걸려올 때만) · 도구 요약 · 로그(힌트+제안 | 말풍선 | 통화 카드 | 질문 카드) · 입력(📞 · 보내기|정지) 또는 통화 바(한 마디 · 끊기)
│  └─ 제어     section.control       머리띠(기록 · 나) + 자리. 대상 선택기 없음(도구 호출이 정함)
│     └─ 자리  div.control-slot      차례 = 잉크 테두리 · 그 외 테두리 없음. 비어 있으면 한 줄(대상 이름 / 연결 끊김 / 뽀미 진행 중). 평상시(데려올 창 없음)엔 자리 대신 기록(타임라인 탭)이 열 전부
└─ 차례 띠     footer.turn           사람 차례일 때만: 질문 한 줄 + 버튼. 제어 열 아래

기록 페이지                          작업 공간을 통째로 대체. 머리띠(대화로 돌아가기) + 탭: 타임라인 · 증빙 · 분개 · 절차 · 건강 · 3D · 상태·비밀
  절차 탭 = 계정과목표 같은 한 나무(Web/playbooks.html): 자리(Catalog/categories.json — 공공은 docs/gov-sites.md 의 부문 › 부처 › 기관, 앱은 금융/생활 분류) › 패키지(대상·버전) › 기능, 줄마다 단계·✓△✗·미검증 수, 파일 탐색기식으로 그 자리에서 펼침; 줄을 고르면 아래에 명세 나무(Web/playbook.js: 같은 앞 단계는 한 줄기로 접힘, 판정 채움, 👤·✋ 점선, 발자국 그래프) + "…에서 열기" + 사람 차례 + 사용법. "트리맵" 토글은 기능마다 미검증 단계 수가 면적(Web/facts.js) = 남은 검증 일. SwiftUI 카드·상세는 지움(가져오기·새로고침 머리만 남김). 탭 줄은 2026-09-11 다시 보임(절차를 열 길이 필요해서); 첫 탭은 여전히 타임라인.
```

상태는 셋: 대화(자리 비어 있음, 진행 중이면 한 줄) · 차례 · 기록. 크기 클래스는 둘: compact(< 840px: 제어 열 → 대화 위 레일 한 줄, 구멍 없음) · expanded(≥ 840px: 위 트리 그대로). 스토리는 셋 × 둘. 대화 셸은 처음 · 대화 중 · 통화 중 · 걸려옴 · 통화 끝 · 오류 · 미설정 · Android 미연결.

## 3. 단순화 (삭제 뒤에만)

- 간격 값 하나(.75rem). 고정 높이 없음. 차례 띠는 있을 때만 높이를 가진다.
- 제어 열 폭 = 대상 창 폭 + 간격 2. 대상이 없으면 최소 폭(iPhone 폭).
- 사람 차례 신호는 띠(질문 + 버튼)가 주고 테두리는 보조다.

## 4. 주기 · 자동화

- 주기: 구조 변경은 스토리북 스토리 셋을 고치는 일이다. 툴바로 테마·글자 크기를 같은 조건에서 본다.
- 자동화: 색은 `agent/scripts/generate-theme.mjs`가 세 플랫폼으로 생성. 레이아웃 값(간격·최소 제어 폭)도 같은 방식으로 생성할 후보.

## 5. 주입 계약

- 서브트리: `ReactNode` prop. 뼈대는 위치만 정한다.
- 스타일: 컴포넌트는 클래스 이름만 낸다. 색·크기는 `tokens.css`, 모양은 `style.css`/`workbench.css`.
- 상태: `main.tsx`만 브리지를 안다. 뼈대에는 값과 `onX`만 들어간다.
- 네이티브: Mac `WorkbenchLayout`·Android `rememberControlSlot`이 같은 트리를 pt/dp로 만든다.
