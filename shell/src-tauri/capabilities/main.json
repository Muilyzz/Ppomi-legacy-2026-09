{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Main window may run one brain/path/body spine command.",
  "windows": ["main"],
  "permissions": ["core:default", "allow-run-path"]
}
