{
  "name": "ppomi-hub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": "24.x" },
  "scripts": {
    "test": "node --test",
    "sync:catalog": "node ../scripts/sync-catalog.mjs",
    "prepare:downloads": "node ../scripts/prepare-downloads.mjs",
    "deploy": "npm run sync:catalog && npm run prepare:downloads && vercel deploy --prod --yes --scope muilyzz"
  },
  "dependencies": { "@vercel/kv": "^3.0.0" }
}
