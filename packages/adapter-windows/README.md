# adapter-windows

Current Windows body driver name. Future: `ppomi-body-windows`. Live UIA on sibling PR #15 is `driver-windows`.

This tip ships the example only:

```sh
npm run example
node --experimental-strip-types example/src/main.ts
```

Off Windows the example prints SKIP and exits 0. See [example/README.md](example/README.md).
