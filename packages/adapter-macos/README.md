# adapter-macos

Current macOS body driver name. Future: `ppomi-body-macos` (`driver-macos`). Sibling implementation PRs: #10 / #12.

This tip ships the example only:

```sh
npm run example
node --experimental-strip-types example/src/main.ts
```

Off macOS the example prints SKIP and exits 0. See [example/README.md](example/README.md).
