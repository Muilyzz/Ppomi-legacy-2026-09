# ppomi-brain

Future orchestration library (MZZ-41). This tip ships only the independently runnable example (mocks by default).

```sh
npm run example
# or
node --experimental-strip-types example/src/main.ts
```

See [example/README.md](example/README.md) and [docs/package-examples.md](../../docs/package-examples.md).
