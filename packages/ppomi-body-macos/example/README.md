# ppomi-body-macos example

One-step `ppomi-body` smoke. Default: fixture `Runtime` click. Live AX skips off macOS.

```sh
node --experimental-strip-types packages/ppomi-body-macos/example/src/main.ts
```
